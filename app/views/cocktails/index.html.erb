<h1>Cocktails</h1>
<%= link_to "Add new cocktail", new_cocktail_path, class: "btn btn-primary" %>
<ul>
  <% @cocktails.each do |cocktail| %>
    <li><%= cocktail.name %> <%= link_to "Show", cocktail_path(cocktail), class: "btn btn-info" %></li>
  <% end %>
</ul>
